---
description: >-
  代码生成与复用、魔法数字；工程目录与文件结构、同域模块聚合、包根配置；
  通用标识符命名（PascalCase/camelCase/kebab-case/UPPER_SNAKE）、路径分段。
  类型前缀与 React 细则见 typescript.mdc、react.mdc。
alwaysApply: true
---

# 通用工程与生成规范

适用于 **代码生成**、**项目/目录优化**、**目录与文件生成**。样式与 CSS Module 见 `react.mdc`；TypeScript 类型前缀见 `typescript.mdc`。

---

## 代码生成

- 可复用逻辑须提炼为公共模块（函数、组件、Hook、工具），避免复制粘贴。
- 重复实现控制在合理范围：**同类逻辑重复率不宜超过约 10%**；明显重复时优先合并。
- 生成前对齐现有目录中的命名、导出方式与类型风格，保持同一作者口吻。
- **避免魔法数字**：状态、阶段等用具名常量或枚举映射，勿在业务逻辑中散落裸数字比较。

```javascript
// 不推荐：魔法数字
if (state === 1 || state === 2) {
}

// 推荐：具名常量
const UNPUBLISHED = 1;
const PUBLISHED = 2;
const DELETED = 3;

if (state === UNPUBLISHED || state === PUBLISHED) {
} else if (state === DELETED) {
}
```

---

## 命名约定（通用）

适用于 TS/JS、Node、脚本及前端工程中的**标识符与路径**命名。

### 标识符

- **PascalCase**：React 组件名、类名、类型名、接口名；**组件目录名**（与组件同名）。
- **camelCase**：变量、函数、方法、属性、一般模块内函数。
- **camelCase + `use` 前缀**：自定义 Hook 文件名与函数名，如 `useAuth`、`useForm`。
- **UPPER_SNAKE_CASE**：环境变量、跨模块常量、全局配置键（语义完整，如 `API_BASE_URL`）。
- **kebab-case**：npm 包名、应用/包根目录、**配置文件名**（如 `electron-builder.json`）、**非组件**的多词工具/服务文件名（如 `note-service.ts`）。

### 事件、布尔与集合

- 事件处理函数：`handle` 前缀，如 `handleClick`、`handleSubmit`。
- 布尔变量：`is` / `has` / `can` 前缀，如 `isLoading`、`hasError`、`canSubmit`。
- 数组：名词 + `List` 或复数，如 `userList`、`items`。

### 用词与缩写

- 使用完整英文单词；允许业界通用缩写：`err`、`req`、`res`、`props`、`ref`。
- **禁止**拼音与英文混排；**禁止**以中文作为标识符（域名、品牌等专有名词除外）。
- 杜绝模糊缩写，保证见名知义。

### 路径与目录分段

- **包或应用根目录**：kebab-case。
- **固定约定目录**：小写，如 `components`、`hooks`、`services`、`utils`、`types`。
- **单个 React 组件根目录**：**PascalCase**（如 `NoteTree/`），入口为 `index.tsx`；勿用名为 `index` 的文件夹代替组件文件夹。
- **功能域、路由段、多词业务分包**：**kebab-case**，例如 `components/auth-wizard` 下再放 PascalCase 组件子目录。
- **Hook 文件**：`useXxx.ts`。
- **类型文件**：业务内聚用同目录 `types.ts`；全包共享类型放 `types/`。

React 组件目录、Props、样式引用等细则见 `react.mdc`。

---

## 项目与目录

### 项目优化

- 调整包边界、依赖、脚本或构建配置时，仍须遵守下文目录与文件命名约定。
- 保持 `package.json`、`tsconfig.json`、说明类文档等位于包根目录。

### 目录与文件生成

- 每个 React 组件独占 **PascalCase** 目录，入口为 `index.tsx`；子组件放在 `components/` 下子目录中。
- 生成 `index.ts` 聚合导出时，与 `src` 约定保持一致，避免循环依赖。

### 同域模块聚合（目录优于长文件名）

- 在 `services`、`utils`、纯逻辑包等位置，同一业务域/子系统（如 Skill、Prompt）应使用 **子目录表达域边界**；勿仅靠域前缀平铺长文件名（如 `skill-xxx.ts`、`skill-yyy.ts`）。
- **域内文件名**宜简短：父目录已表域（如 `services/skill/`），子文件可用 `platform-sync.ts`、`stats.ts` 等。
- **反例**：`services/skill-platform-sync.ts`、`services/skill-stats.ts` 平铺。
- **正例**：`services/skill/platform-sync.ts`、`services/skill/stats.ts`。
- 新增同域能力优先落在既有域目录下；单目录文件过多时再分子目录，避免过深无意义嵌套。

### 目录规范（参考树）

```
my-notes-package/                                       # 包根目录
├── package.json                                        # npm 包配置
├── tsconfig.json                                       # TypeScript 配置
├── README.md                                           # 包说明文档
├── docs/                                               # 文档目录
├── example/                                            # 示例代码
├── scripts/                                            # 运行脚本
└── src/                                                # 源代码目录
    ├── components/                                     # 可复用通用组件目录
    │   └── NoteTree/                                   # 组件名称，PascalCase 格式
    │       ├── components/                             # 子组件目录
    │       │   └── TreeChildren/                       # 子组件名称，PascalCase 格式
    │       │       ├── index.tsx                       # 子组件入口
    │       │       └── index.module.less               # 子组件模块样式文件
    │       ├── index.tsx                               # 组件入口
    │       ├── index.module.less                       # 组件模块样式文件
    │       ├── types.ts                                # 类型定义
    │       └── utils.ts                                # 当前组件工具函数
    ├── hooks/
    │   └── useNoteTree.ts                              # 自定义 Hook
    ├── services/
    │   ├── noteService.ts                              # 单域或跨域入口级服务
    │   └── skill/                                      # 同域多能力：用目录聚合
    │       ├── platform-sync.ts
    │       └── stats.ts
    ├── utils/
    │   └── treeUtils.ts                                # 通用工具函数
    ├── types/                                          # 通用类型
    └── index.ts                                        # 包导出入口
```

### 规范说明

1. **目录命名**
   - 与上文 **「命名约定（通用）→ 路径与目录分段」** 一致：包根 **kebab-case**；`hooks` / `services` / `utils` / `components` 等固定目录小写；**组件根目录 PascalCase**；功能域、路由段可用 **kebab-case**。
   - 同类逻辑用子目录聚合，如 `skills/a.ts`、`skills/b.ts`，禁止 `skills-a.ts`、`skills-b.ts`。
   - **同域多文件**须遵守上文 **「同域模块聚合」**：优先 `services/skill/stats.ts`，避免在 `services/` 根下堆 `skill-stats.ts`、`skill-platform-sync.ts` 等仅靠文件名区分的平铺。
   - 独立业务类型放在对应业务目录的 `types.ts`；通用类型放在根目录 `types/`。

2. **文件命名**
   - 组件对外名称用 **PascalCase**；磁盘入口优先 **`NoteTree/index.tsx`**，勿在 `components` 下平铺 `NoteTree.tsx`。
   - 自定义 Hook：`useXxx.ts`；样式：`index.module.less`；类型：`types.ts`。
   - 工具/服务/配置：`xxx.ts` 或小写短横线，如 `electron-builder.json`、`note-service.ts`；**域内**服务/工具文件名在父目录已表域时可缩短（见「同域模块聚合」）。

3. **结构说明**
   - `docs`：项目文档。
   - `example`：示例。
   - `scripts`：构建与运行脚本。
   - `src/components`：UI 组件；`src/components/NoteTree/components`：子组件。
   - `src/hooks`：自定义 Hook 与组合逻辑。
   - `src/services`：接口适配与 CRUD。
   - `src/utils`：纯函数与通用逻辑。
   - `src/index.ts`：包级导出。

4. **备注**
   - 树形数据与 UI 分离：`services` 负责数据与 CRUD，`components` 负责展示与交互。
   - 保持 `package.json`、`tsconfig.json`、`README.md` 等在包根，便于包维护与发布。
