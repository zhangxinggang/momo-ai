---
description: >-
  React/TSX 组件架构、Hooks、性能优化、JSX 书写；CSS Module、Less、Ant Design；
  代码风格、错误处理、空值处理、逻辑判断与前端性能优化。
alwaysApply: false
globs:
  - "**/*.tsx"
  - "**/*.jsx"
---

# React 与现代 Web 开发规范

本指南聚焦 **React + TypeScript + 现代前端**（JSX、Hooks、Ant Design、Less/CSS Module）。

**命中方式**：`alwaysApply: false` 时，Cursor 结合 **description** 与 **globs**（`*.tsx` / `*.jsx`）附加规则；编辑纯 `.ts` Hook 或未匹配 glob 时，可 **@ 引用本规则**。

- 通用命名、目录树、魔法数字 → `common.mdc`
- 类型前缀、`IProps`、D/E/I 约定 → `typescript.mdc`

---

## 开发理念

- 编写干净、可维护、可扩展的代码；遵循 SOLID 原则。
- 函数式、声明式编程优于命令式；强调类型安全与组件驱动开发。

---

## 代码风格

- 使用制表符缩进；单引号字符串；省略分号（除非消除歧义）。
- 消除未使用变量；关键词后、函数括号前、中缀运算符两侧加空格；逗号后加空格。
- 始终使用 `===`；`else` 与闭合花括号同一行；多行 `if` 使用花括号。
- 回调中处理错误参数；行长度建议 80 字符内；多行对象/数组使用尾随逗号。

---

## 命名约定（React 补充）

- **Props 类型名**：固定 **`IProps`**（见 `typescript.mdc`）。
- **Props 与 DOM 属性**：**camelCase**，与 React/DOM 惯例一致。
- **组件显示名**：必要时设置 `displayName`；默认以文件夹名（PascalCase）为准。
- 组件/Hook/事件/布尔/目录分段等通用命名见 `common.mdc`。

---

## 组件架构

- 使用带 TypeScript 接口的**函数组件**；用 `function` 关键字定义组件。
- 可复用逻辑提取到自定义 Hook；实现合适的组件组合。
- 策略性使用 `React.memo()`；`useEffect` 中实现适当清理。
- 保持组件职责单一；函数组件 + Hooks 优先于 class 组件。

### 组件目录与入口

- 每个组件独占 **PascalCase** 目录，入口 **`index.tsx`**；样式 **`index.module.less`**。
- 目录名勿用 `index`；子组件放在 `components/` 下继续按目录拆分。

### JSX 书写

- 每个标签属性**换行**书写：

```jsx
<Component
  prop1="value1"
  prop2="value2"
/>
```

- 列表渲染必须绑定 **key**（避免用索引作 key）。
- 避免 JSX 内联函数、内联样式与内联事件；模板保持简洁。

---

## 样式（CSS Module + Less）

### 通用原则

- 优先 **CSS Module**（`index.module.less`），避免全局污染。
- **类名 kebab-case**（如 `.chat-panel`）；TSX 中通过 `styles['kebab-name']` 引用。
- **层级与 DOM 对齐**：根块作命名空间（如 `.skill`），子层链式前缀（`.skill-header`、`.skill-body-main`）；避免同级大量孤立短名。
- 样式文件与组件同目录；Less 嵌套不超过 5 层；移动优先、响应式；满足无障碍对比度。
- 选择器优先**类选择器**，避免标签名；使用直接子选择器 `>`；尽量避免 ID 与全局标签选择器。
- 多个 `className` 时使用 **classnames** 库；深度改子组件样式时，一个类名下只用一个 `:global`。

### 引用示例

```tsx
import styles from './index.module.less';
import classNames from 'classnames';

function RightPanel() {
  return (
    <div className={styles.right}>
      <div className={styles['right-layout']}>布局</div>
    </div>
  );
}
```

```less
// index.module.less
.right {
  &-layout {
    position: relative;
  }
}
```

```tsx
// 错误：直接使用字符串类名
<div className="right" />
```

### Ant Design

- 使用 AntD 实现一致、可访问的 UI；组合模式创建模块化、可复用组件。

---

## 性能优化

### React

- `useCallback` 记忆回调；`useMemo` 记忆昂贵计算。
- 动态导入实现代码分割；列表 key 使用稳定唯一标识。

### 资源与 DOM

- 图片使用合适尺寸与格式；非首屏图片 `loading="lazy"`。
- 减少 DOM 操作与样式修改；批量 DOM 变更可先 `display: none` 移出文档流。
- 频繁操作（输入、滚动、点击）使用节流/防抖。

---

## JavaScript 规范

- 优先 ES6+：`const` / `let`、箭头函数、模板字符串、解构；避免 `var`。
- 箭头函数优先用于回调与事件处理。
- 条件与循环嵌套最多三层；慎用 `console.log`。

---

## 错误处理

### 错误边界

```javascript
class ErrorBoundary extends React.Component {
  componentDidCatch(error, errorInfo) {
    console.error('Caught an error:', error, errorInfo);
  }
}
```

### 异步与 Promise

- 异步请求使用 `try/catch`。
- 所有 Promise 须处理 `.catch`，勿忽略潜在错误。

---

## 空值处理

- 可选链：`user?.profile?.avatar`；`store?.getters?.userInfo?.menus`。
- 空值合并：`const value = input ?? 'default'`。
- 可选回调：`props?.onChange?.(e)`；或解构默认值 `const { onChange = NOOP } = props`。

---

## 逻辑判断

- 始终 `===`；复杂嵌套用**早返回**（异常前置、正常后置）。
- 简单条件可用三元；避免嵌套三元。
- 复杂条件抽成命名函数；分支逻辑优先常量映射表：

```javascript
const STATUS_MAP = {
  success: 'Successfully',
  fail: 'failed',
  danger: 'dangerous',
};
return STATUS_MAP[status] ?? status;
```

---

## 注释

- 公共 API 使用 JSDoc；复杂逻辑与边界处理加注释。
- 避免显而易见的注释（如 `let x = 1; // 将 x 设置为 1`）。
