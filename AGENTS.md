# 架构文档规则

每次完成代码、配置或文档修改后，先判断是否影响系统架构。以下任一变化视为架构变更：模块或包边界、依赖关系、跨进程或跨服务通信、数据流、持久化、外部集成、部署或运行时拓扑、对外接口契约。

- 发生架构变更时，必须在当前项目的 `docs/architecture/` 中按受影响项目新增或更新架构文档；同一次修改内完成，不能只在最终答复中说明。
- 架构图必须使用 Mermaid，并以当前代码为准。图应优先表达模块边界和关键数据/控制流；每张图只配简短说明，不写大段重复描述。
- 系统较大时，先维护一个系统总览图，再按项目或关键链路拆分小图。文档文件名使用 kebab-case，例如 `system-overview.md`、`agent-runtime.md`。
- 未影响架构时无需改动架构图；不要为普通实现细节制造图表噪声。
- 完成前检查相关 Mermaid 图是否仍覆盖本次变更，并在最终答复中列出更新的架构文档。
